[project]
name = "openenv-hackathon"
version = "0.1.0"
description = "OpenEnv submission"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
